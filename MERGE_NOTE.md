# Monorepo merge complete (2026-06-15)

The `PokemonChampBot_Singles` duplicate workspace was merged into this repo after
the 500k-step VGC RL training run finished. Layout: `src/core/`, `src/doubles/`,
`src/singles/`.

The duplicate folder `PokemonChampBot_Singles` can be deleted when no longer needed.
